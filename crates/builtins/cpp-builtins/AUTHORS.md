The `*.{cpp,hpp,h}` portions of the code found in this directory are copied from previous implementation of the StarlingMonkey runtime. Their full history and authors attribution can be seen in the commit log of the `/builtins`, `/runtime`, and `/host-api` directories as existed in git tag `v0.2.1`.

For these files, the initial commit are Copyright © 2021-2023 Fastly Inc. and contributors to Fastly's
[js-compute-runtime project](https://github.com/fastly/js-compute-runtime/).

Changes following that initial commit, as well as all other files not derived from those described above, are Copyright © the respective contributors.
